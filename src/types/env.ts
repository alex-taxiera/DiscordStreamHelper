export interface ENV {
  STREAM_DISCORD_TOKEN: string
  STREAM_DB_CLIENT: string
  STREAM_DB_NAME: string
  STREAM_DB_USER: string
  STREAM_DB_PASS: string
  STREAM_DB_HOST: string
  STREAM_DB_CONNECTION: string
}
